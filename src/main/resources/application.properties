spring.application.name=PhotoConnect

# Database Configuration
# TODO: Use environment variables for production: ${DB_URL:jdbc:mysql://localhost:3306/PhotoConnect?createDatabaseIfNotExist=true}
spring.datasource.url=jdbc:mysql://localhost:3306/PhotoConnect?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Qwerty@1234
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# JWT Configuration
# TODO: Use environment variable for production: ${JWT_SECRET:default-secret}
jwt.secret=${JWT_SECRET:xoU9LUg1Z13euOqlaNJLnwYEDYFgO1O6}
jwt.expiration=86400000

# Mail Configuration (Gmail)
# TODO: Use environment variables for production
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:kaarthiraghav@gmail.com}
spring.mail.password=${MAIL_PASSWORD:klxeuczdekunnqde}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=false
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# App base URL (used for emails / magic links)
# app.url=http://localhost:8080
